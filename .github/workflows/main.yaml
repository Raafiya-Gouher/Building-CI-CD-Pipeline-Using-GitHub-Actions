name: Build, Test, and Deploy

on:
  push:
    branches:
      - main

permissions:
  contents: read
  issues: write  

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout code
      - name: Checkout code
        uses: actions/checkout@v3

      # Step 2: Set up Python
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      # Step 3: Install dependencies for CI
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest

      # Step 4: Run unit tests
      - name: Run unit tests
        run: |
          PYTHONPATH=$(pwd) pytest tests/ 

      # Step 5: Await manual approval before deployment
      - name: Await manual approval
        uses: trstringer/manual-approval@v1
        with:
          approvers: ${{ secrets.DEPLOY_APPROVER }}
          secret: ${{ github.TOKEN }}
          issue-title: "Deploy approval needed"
          issue-body: "Please approve deployment to production."

      # Step 6: Deploy entire repo to EC2 with venv
      - name: Deploy to EC2
        if: ${{ success() }}
        env:
          EC2_IP: ${{ secrets.EC2_HOST }}
          EC2_SSH_KEY: ${{ secrets.EC2_SSH_KEY }}
        run: |
          echo "Deploying full repository to EC2..."
          echo "$EC2_SSH_KEY" > ec2-key.pem
          chmod 600 ec2-key.pem

          # Ensure remote app directory exists
          ssh -o StrictHostKeyChecking=no -i ec2-key.pem ubuntu@$EC2_IP "mkdir -p /home/ubuntu/app"

          # Copy full repo to EC2
          rsync -avz -e "ssh -o StrictHostKeyChecking=no -i ec2-key.pem" ./ ubuntu@$EC2_IP:/home/ubuntu/app/

          # Deploy app with virtual environment
          ssh -o StrictHostKeyChecking=no -i ec2-key.pem ubuntu@$EC2_IP << 'EOF'
            cd /home/ubuntu/app

            # Create or reuse virtual environment
            python3 -m venv venv
            source venv/bin/activate

            # Install dependencies inside venv
            pip install --upgrade pip
            pip install -r requirements.txt

            # Restart app
            pkill -f app.py || true
            nohup python app.py > app.log 2>&1 &
          EOF
          echo "Deployment to EC2 completed."